---
# WARNING:
# The contents of this file are entirely experimental, may change at any time,
# and SHOULD NOT be depended on in any way.
#
# The files generated from this YAML file (e.g. JSON files hosted on GH Pages)
# are also entirely experimental, may change at any time,
# and SHOULD NOT be depended on in any way.
releases:
  2026.02.20.1:
    jar_version: 3.7.13+red.1
    yt_plugin_version: 1.13.5
    java_versions: [17, 21]
    min_red_version: 3.5.23
    release_stream: stable
